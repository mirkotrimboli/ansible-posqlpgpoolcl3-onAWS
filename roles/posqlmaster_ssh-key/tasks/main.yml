---
- name: Set authorized key taken from file
  authorized_key:
    user: root
    state: present
    key: "{{ lookup('file', '~/.ssh/id_rsa_pgpool.pub') }}"

- name: Set authorized key taken from file
  authorized_key:
    user: postgres
    state: present
    key: "{{ lookup('file', '~/.ssh/id_rsa_pgpool.pub') }}"
